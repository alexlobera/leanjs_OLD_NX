
import { getURLParameter } from './url'

describe('getURLParameter', () => {
	it('should decode URI components', () => {

	})
	it('should extract parameters from the query string ', () => {
		
	})
	it('should not extract parameters from the path ', () => {
		
	})
	it('should interpret the + sign as a space', () => {
		
	})
	it('should interpret %20 as a space', () => {
		
	})
	it('should not match any of the following characters: [&;]', () => {
		
	})
})

