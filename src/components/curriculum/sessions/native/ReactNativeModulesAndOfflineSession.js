import React from 'react'
import Ul, { Li } from '../../../layout/Ul'
import Session from '../Session'

const ReactNativeModulesAndOfflineSession = ({ title }) => (
  <Session title={title}>
    <Ul>
      <Li>...</Li>
    </Ul>
  </Session>
)

export default ReactNativeModulesAndOfflineSession
