import React from 'react'
import { shallow } from 'enzyme'
import { shallowToJson } from 'enzyme-to-json'

import AboutUs from './about-us'

describe('<AboutUs />', () => {
  it('should render page', () => {
    //add basic test with no snapshot
  })
})
