import React from 'react';
import Box from '../layout/Box';
import Flex from '../layout/Flex';

export const Label = (props) => <Box as="label" {...props} />;

export const FlexLabel = (props) => <Flex as="label" {...props} />;

export default Label;
